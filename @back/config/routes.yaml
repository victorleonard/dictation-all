controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# La route /api/auth est gérée automatiquement par Symfony Security (json_login)
# Pas besoin de la définir ici
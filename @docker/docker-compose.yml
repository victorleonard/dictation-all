services:
  # Backend Symfony
  backend:
    build:
      context: ..
      dockerfile: "@docker/Dockerfile.back"
    container_name: dictation_backend
    restart: unless-stopped
    volumes:
      - ../@back:/app
      # Volume pour les logs et cache
      - backend_var:/app/var
    environment:
      - APP_ENV=${APP_ENV:-prod}
      - APP_DEBUG=${APP_DEBUG:-0}
      - DATABASE_URL=${DATABASE_URL:-mysql://dictation:!ChangeMe!@database:3306/dictation?serverVersion=8.0&charset=utf8mb4}
      - CORS_ALLOW_ORIGIN=${CORS_ALLOW_ORIGIN:-^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - app-network

  # Frontend Quasar
  frontend:
    build:
      context: ..
      dockerfile: "@docker/Dockerfile.front"
      args:
        # URL de l'API backend - accessible depuis le navigateur du client
        # Le JavaScript s'exécute dans le navigateur, donc on utilise localhost:3000
        VITE_API_BASE_URL: http://localhost:3000/
    container_name: dictation_frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    depends_on:
      - backend
      - nginx
    networks:
      - app-network

  # Base de données MySQL
  # Utilisation de MySQL pour correspondre aux migrations existantes
  database:
    image: mysql:${MYSQL_VERSION:-8.0}
    container_name: dictation_database
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-dictation}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-!ChangeMe!}
      MYSQL_USER: ${MYSQL_USER:-dictation}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-!ChangeMe!}
    volumes:
      - ../@data/mysql:/var/lib/mysql:rw
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-!ChangeMe!}"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: --default-authentication-plugin=mysql_native_password

  # Nginx pour le backend
  nginx:
    image: nginx:alpine
    container_name: dictation_nginx
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-3000}:80"
    volumes:
      - ../@back/public:/var/www/html/public:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  backend_var:

networks:
  app-network:
    driver: bridge

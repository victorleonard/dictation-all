# Dockerfile pour le frontend Quasar (Développement)
FROM node:20-alpine

# Installer les dépendances système
RUN apk add --no-cache git bash

# Définir le répertoire de travail
WORKDIR /app

# Exposer le port de développement Quasar (par défaut 9000)
EXPOSE 9000

# Commande par défaut (sera surchargée par docker-compose)
CMD ["npm", "run", "dev"]
